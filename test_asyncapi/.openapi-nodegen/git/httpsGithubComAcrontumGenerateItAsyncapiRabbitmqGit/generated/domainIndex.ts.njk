{% set ids = getChannelPublishOperationIds('subscribe', nodegenRc, swagger.channels) -%}
{% for id in ids -%}
{% set domainName = ucFirst([_.camelCase(id), 'Domain'] | join) -%}
import {{ domainName }} from '../domains/{{ domainName }}';
{% endfor -%}

export {
  {% for id in ids -%}
  {% set domainName = ucFirst([_.camelCase(id), 'Domain'] | join) -%}
  {{ domainName }},
  {% endfor -%}
};
