{% for endpoint in endpoints -%}
{% set channelDomain = ucFirst(prettifyRouteName(endpoint)) + 'Channel' -%}
import {{channelDomain}} from './{{ucFirst(prettifyRouteName(endpoint))}}Channel';
{% endfor -%}

export {
  {% for endpoint in endpoints -%}
  {% set channelDomain = ucFirst(prettifyRouteName(endpoint)) + 'Channel' -%}
  {{ channelDomain }},
{% endfor %}};
